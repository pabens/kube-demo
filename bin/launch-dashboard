#!/bin/bash
# create the dashboard

kubectl create -f cfg/kubernetes-dashboard.yaml

echo
echo kubernetes dashboard available via kube proxy at...
echo http://localhost:8080/api/v1/proxy/namespaces/kube-system/services/kubernetes-dashboard/#/workload
echo
